ChatGPT：
这是一个 C++程序，用于计算从第一天开始的给定天数内，骑士一共获得了多少金币。

程序首先包含了必要的头文件，然后定义了变量 n，a 和 b，其中 n 表示给定的天数，a 和 b 用于计算每天的工资和累计工资。

在 main 函数中，首先读取输入的 n，然后使用 for 循环遍历从 1 到 n 的每一天。在循环中，每天的工资被累加到 sum 中，同时检查当前的 a 是否等于 b，如果是，则将 a 加 1，并将 b 重置为 1，否则将 b 加 1。这样做的目的是确保每组的连续天数都能被正确计算。

最后，输出累计工资 sum。

这个程序简洁明了，没有过多的解释，符合你的要求。